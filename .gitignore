# Python
__pycache__/
*.py[cod]
*$py.class

# Виртуальные окружения (проект использует только stdlib, но на случай, если кто-то заведёт venv)
venv/
.venv/
env/
ENV/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Локальные настройки Claude Code (личные, не для общего репозитория)
.claude/settings.local.json

# Личный конфиг станции с реальными IP-адресами — создаётся каждым при разворачивании,
# в репозиторий не коммитится (в отличие от шаблонов/примеров в configs/)
/config.json
